# This is Git's per-user configuration file.

[user]
name = Jonas Collberg

[github]
user = joncol

[includeIf "gitdir:/home/jco/code/"]
path = config.code

[includeIf "gitdir:/home/jco/work/"]
path = config.work

[alias]
br = branch
ci = commit
co = checkout
last = "log -1 HEAD"
st = status
sta = status
unstage = "reset HEAD --"
lola = "log --graph --decorate --pretty=lola --abbrev-commit --branches --remotes"

[magit]
hideCampaign = true

[pretty]
lola = %h%C(red)%d%Creset% s

[rebase]
autosquash = true
autostash = true

[remote "origin"]
prune = true
