use nix -j4 -A devel-shell nix/shell.nix # Use this to disable `haskell-language-server`.
# use nix -j4 -A ide-shell nix/shell.nix # Use this to enable `ghcid` and `haskell-language-server`.

if [ -e .envrc-local ]; then
   source .envrc-local
fi

export PATH=~/.cabal/bin:$PATH
